<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<template id="__bundler_thumbnail" data-bg-color="#E7E9E8">
  <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
    <rect x="30" y="6" width="40" height="88" rx="8" fill="#F6F7F7" stroke="#14201D" stroke-width="2"/>
    <rect x="36" y="16" width="28" height="6" rx="2" fill="#14201D"/>
    <rect x="36" y="28" width="28" height="3" rx="1.5" fill="#8A928F"/>
    <rect x="36" y="34" width="20" height="3" rx="1.5" fill="#8A928F"/>
  </svg>
</template>
<script src="./support.js"></script>
</head>
<body>
<x-dc>
<helmet>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-database-compat.js"></script>
  <style>
    body { margin: 0; }
    * { box-sizing: border-box; }
    ::-webkit-scrollbar { display: none; }
  </style>
</helmet>
<div style="width:100%;min-height:100vh;background:#E7E9E8;display:flex;align-items:center;justify-content:center;padding:40px 20px;font-family:-apple-system,'Helvetica Neue',Arial,sans-serif;">
  <x-import component-from-global-scope="IOSDevice" from="./ios-frame.jsx" width="402" height="874" hint-size="402,874">
    <div style="height:100%;display:flex;flex-direction:column;position:relative;background:#F6F7F7;padding-top:56px;overflow:hidden;">

      <!-- ============ LOGIN ============ -->
      <sc-if value="{{ isLogin }}" hint-placeholder-val="{{ false }}">
        <div style="flex:1;display:flex;flex-direction:column;justify-content:center;padding:0 26px;">
          <img src="./assets/logo-la-emilia.png" style="height:20px;width:auto;display:block;margin-bottom:26px;" />
          <div style="font-size:22px;font-weight:700;letter-spacing:-0.3px;margin-bottom:2px;">Hola, chofer</div>
          <div style="font-size:14px;color:#707A76;margin-bottom:26px;">Ingresá con tu cuenta del TMS</div>

          <div style="display:flex;flex-direction:column;gap:12px;">
            <input type="email" value="{{ loginEmail }}" onChange="{{ onLoginEmailChange }}" onKeyDown="{{ onLoginKeyDown }}" placeholder="Email" style="width:100%;padding:13px 14px;border-radius:12px;border:1.5px solid #E0E4E2;font-size:15px;font-family:inherit;color:#14201D;background:#fff;" />
            <input type="password" value="{{ loginPass }}" onChange="{{ onLoginPassChange }}" onKeyDown="{{ onLoginKeyDown }}" placeholder="Contraseña" style="width:100%;padding:13px 14px;border-radius:12px;border:1.5px solid #E0E4E2;font-size:15px;font-family:inherit;color:#14201D;background:#fff;" />

            <sc-if value="{{ loginError }}" hint-placeholder-val="{{ false }}">
              <div style="font-size:13px;color:#B23A2C;background:#F8DEDB;padding:10px 12px;border-radius:10px;">{{ loginError }}</div>
            </sc-if>

            <div onClick="{{ onLoginSubmit }}" style="text-align:center;padding:15px;border-radius:12px;background:{{ loginButtonBg }};color:#fff;font-weight:700;font-size:15px;cursor:{{ loginButtonCursor }};margin-top:4px;">{{ loginButtonLabel }}</div>

            <div style="text-align:center;font-size:12px;color:{{ fbStatusColor }};margin-top:10px;display:flex;align-items:center;justify-content:center;gap:6px;">
              <div style="width:7px;height:7px;border-radius:4px;background:{{ fbStatusColor }};"></div>{{ fbStatusLabel }}
            </div>
          </div>
        </div>
      </sc-if>

      <sc-if value="{{ isLoggedIn }}" hint-placeholder-val="{{ false }}">
      <!-- ============ HEADERS ============ -->
      <sc-if value="{{ isRuta }}" hint-placeholder-val="{{ true }}">
        <div style="padding:6px 20px 16px;flex-shrink:0;">
          <div style="display:flex;align-items:center;gap:8px;margin-bottom:14px;">
            <img src="./assets/logo-la-emilia.png" style="height:18px;width:auto;" />
          </div>
          <div style="display:flex;align-items:center;justify-content:space-between;">
            <div style="font-size:22px;font-weight:700;color:#14201D;letter-spacing:-0.3px;">Hola, {{ driverName }}</div>
            <div onClick="{{ onLogout }}" style="font-size:12px;font-weight:700;color:#8A928F;cursor:pointer;padding:6px 10px;border-radius:8px;border:1.5px solid #E0E4E2;flex-shrink:0;">Salir</div>
          </div>
          <div style="font-size:14px;color:#707A76;margin-top:2px;">{{ routeDate }} · {{ vehicleType }}</div>
          <div style="display:flex;align-items:center;gap:6px;margin-top:8px;">
            <div style="width:7px;height:7px;border-radius:4px;background:{{ syncDotColor }};flex-shrink:0;"></div>
            <div style="font-size:12px;color:#8A928F;">{{ syncLabel }}</div>
          </div>
          <div style="margin-top:14px;">
            <div style="display:flex;justify-content:space-between;font-size:13px;color:#4C5652;margin-bottom:6px;">
              <span>{{ progressLabel }}</span>
              <span>{{ progressPctLabel }}</span>
            </div>
            <div style="height:8px;border-radius:5px;background:#E3E6E5;overflow:hidden;">
              <div style="height:100%;border-radius:5px;background:{{ accentColor }};width:{{ progressBarWidth }};transition:width 0.4s ease;"></div>
            </div>
          </div>
        </div>
      </sc-if>

      <sc-if value="{{ isMapa }}" hint-placeholder-val="{{ false }}">
        <div style="padding:6px 20px 12px;flex-shrink:0;">
          <div style="font-size:22px;font-weight:700;color:#14201D;letter-spacing:-0.3px;">Mapa de ruta</div>
          <div style="font-size:14px;color:#707A76;margin-top:2px;">{{ routeDate }} · {{ progressLabel }}</div>
        </div>
      </sc-if>

      <sc-if value="{{ isHistorial }}" hint-placeholder-val="{{ false }}">
        <div style="padding:6px 20px 12px;flex-shrink:0;">
          <div style="font-size:22px;font-weight:700;color:#14201D;letter-spacing:-0.3px;">Historial</div>
          <div style="font-size:14px;color:#707A76;margin-top:2px;">Entregas resueltas hoy</div>
        </div>
      </sc-if>

      <sc-if value="{{ isDetail }}" hint-placeholder-val="{{ false }}">
        <div style="display:flex;align-items:center;gap:12px;padding:6px 16px 14px;flex-shrink:0;">
          <div onClick="{{ onCloseDetail }}" style="width:36px;height:36px;border-radius:18px;background:#fff;display:flex;align-items:center;justify-content:center;box-shadow:0 1px 4px rgba(0,0,0,0.08);cursor:pointer;flex-shrink:0;">
            <svg width="10" height="17" viewBox="0 0 10 17" fill="none"><path d="M8.5 1.5L1.5 8.5l7 7" stroke="#14201D" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <div>
            <div style="font-size:12px;color:#8A928F;font-weight:600;letter-spacing:0.3px;">PARADA {{ currentStop.seq }}</div>
            <div style="font-size:18px;font-weight:700;color:#14201D;">{{ currentStop.customer }}</div>
          </div>
        </div>
      </sc-if>

      <sc-if value="{{ isConfirm }}" hint-placeholder-val="{{ false }}">
        <div style="display:flex;align-items:center;gap:12px;padding:6px 16px 14px;flex-shrink:0;">
          <div onClick="{{ onBackToDetail }}" style="width:36px;height:36px;border-radius:18px;background:#fff;display:flex;align-items:center;justify-content:center;box-shadow:0 1px 4px rgba(0,0,0,0.08);cursor:pointer;flex-shrink:0;">
            <svg width="10" height="17" viewBox="0 0 10 17" fill="none"><path d="M8.5 1.5L1.5 8.5l7 7" stroke="#14201D" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <div style="font-size:18px;font-weight:700;color:#14201D;">Confirmar entrega</div>
        </div>
      </sc-if>

      <sc-if value="{{ isFail }}" hint-placeholder-val="{{ false }}">
        <div style="display:flex;align-items:center;gap:12px;padding:6px 16px 14px;flex-shrink:0;">
          <div onClick="{{ onBackToDetail }}" style="width:36px;height:36px;border-radius:18px;background:#fff;display:flex;align-items:center;justify-content:center;box-shadow:0 1px 4px rgba(0,0,0,0.08);cursor:pointer;flex-shrink:0;">
            <svg width="10" height="17" viewBox="0 0 10 17" fill="none"><path d="M8.5 1.5L1.5 8.5l7 7" stroke="#14201D" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <div style="font-size:18px;font-weight:700;color:#14201D;">No se pudo entregar</div>
        </div>
      </sc-if>

      <!-- ============ SCROLLABLE BODY ============ -->
      <div style="flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;">

        <!-- RUTA LIST -->
        <sc-if value="{{ isRuta }}" hint-placeholder-val="{{ true }}">
          <div style="padding:4px 20px 24px;display:flex;flex-direction:column;gap:10px;">
            <sc-for list="{{ routeList }}" as="stop" hint-placeholder-count="4">
              <div onClick="{{ stop.onOpen }}" style="background:#fff;border-radius:16px;padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer;border:1.5px solid {{ stop.borderColor }};box-shadow:0 1px 3px rgba(20,32,29,0.05);">
                <div style="width:34px;height:34px;border-radius:10px;background:{{ stop.numBg }};color:{{ stop.numColor }};display:flex;align-items:center;justify-content:center;font-weight:700;font-size:14px;flex-shrink:0;">{{ stop.seq }}</div>
                <div style="flex:1;min-width:0;">
                  <div style="font-size:15px;font-weight:600;color:#14201D;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ stop.customer }}</div>
                  <div style="font-size:13px;color:#7B837F;margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ stop.address }}</div>
                  <div style="font-size:12px;color:#9AA19D;margin-top:3px;">{{ stop.window }}</div>
                </div>
                <div style="display:flex;flex-direction:column;align-items:flex-end;gap:6px;flex-shrink:0;">
                  <div style="font-size:11px;font-weight:700;padding:4px 9px;border-radius:20px;background:{{ stop.meta.bg }};color:{{ stop.meta.color }};white-space:nowrap;">{{ stop.meta.label }}</div>
                  <sc-if value="{{ stop.isNext }}" hint-placeholder-val="{{ false }}">
                    <div style="font-size:10px;font-weight:700;color:{{ accentColor }};letter-spacing:0.3px;">PRÓXIMA</div>
                  </sc-if>
                </div>
              </div>
            </sc-for>
          </div>
        </sc-if>

        <!-- MAPA -->
        <sc-if value="{{ isMapa }}" hint-placeholder-val="{{ false }}">
          <div style="padding:0 20px 20px;">
            <div style="position:relative;height:270px;border-radius:20px;overflow:hidden;background:repeating-linear-gradient(135deg,#EDF0EF,#EDF0EF 10px,#E4E8E6 10px,#E4E8E6 20px);border:1px solid #E0E4E2;">
              <div style="position:absolute;top:10px;left:12px;font-family:'SF Mono',Consolas,monospace;font-size:10px;color:#9AA19D;letter-spacing:0.3px;">MAPA — VISTA DE RECORRIDO</div>
              <svg style="position:absolute;inset:0;width:100%;height:100%;" viewBox="0 0 100 100" preserveAspectRatio="none">
                <polyline points="{{ mapPolylinePoints }}" fill="none" stroke="{{ accentColor }}" stroke-width="0.8" stroke-dasharray="2,2" opacity="0.55" />
              </svg>
              <sc-for list="{{ mapPins }}" as="pin" hint-placeholder-count="6">
                <div style="{{ pin.style }}">{{ pin.seq }}</div>
              </sc-for>
            </div>

            <div style="display:flex;gap:14px;margin:14px 0 18px;font-size:11px;color:#7B837F;">
              <div style="display:flex;align-items:center;gap:5px;"><div style="width:9px;height:9px;border-radius:5px;background:{{ accentColor }};"></div>Entregado</div>
              <div style="display:flex;align-items:center;gap:5px;"><div style="width:9px;height:9px;border-radius:5px;background:#C98A1A;"></div>Pendiente</div>
              <div style="display:flex;align-items:center;gap:5px;"><div style="width:9px;height:9px;border-radius:5px;background:#C0392B;"></div>No entregado</div>
            </div>

            <sc-if value="{{ hasNextStop }}" hint-placeholder-val="{{ false }}">
              <div style="background:#fff;border-radius:16px;padding:16px;box-shadow:0 1px 3px rgba(20,32,29,0.05);">
                <div style="font-size:11px;font-weight:700;color:{{ accentColor }};letter-spacing:0.3px;margin-bottom:6px;">PRÓXIMA PARADA</div>
                <div style="font-size:16px;font-weight:700;color:#14201D;">{{ nextStop.customer }}</div>
                <div style="font-size:13px;color:#7B837F;margin-top:3px;">{{ nextStop.address }}</div>
                <div style="font-size:12px;color:#9AA19D;margin-top:3px;margin-bottom:14px;">{{ nextStop.window }}</div>
                <div onClick="{{ onNavigate }}" style="background:{{ accentColor }};color:#fff;text-align:center;padding:13px;border-radius:12px;font-weight:700;font-size:15px;cursor:pointer;">Iniciar navegación</div>
              </div>
            </sc-if>
          </div>
        </sc-if>

        <!-- HISTORIAL -->
        <sc-if value="{{ isHistorial }}" hint-placeholder-val="{{ false }}">
          <div style="padding:4px 20px 24px;display:flex;flex-direction:column;gap:10px;">
            <sc-for list="{{ historialList }}" as="stop" hint-placeholder-count="3">
              <div onClick="{{ stop.onOpen }}" style="background:#fff;border-radius:16px;padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer;box-shadow:0 1px 3px rgba(20,32,29,0.05);">
                <div style="width:34px;height:34px;border-radius:10px;background:{{ stop.meta.bg }};color:{{ stop.meta.color }};display:flex;align-items:center;justify-content:center;font-weight:700;font-size:14px;flex-shrink:0;">{{ stop.seq }}</div>
                <div style="flex:1;min-width:0;">
                  <div style="font-size:15px;font-weight:600;color:#14201D;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ stop.customer }}</div>
                  <div style="font-size:13px;color:#7B837F;margin-top:2px;">{{ stop.resolvedAt }}</div>
                </div>
                <div style="font-size:11px;font-weight:700;padding:4px 9px;border-radius:20px;background:{{ stop.meta.bg }};color:{{ stop.meta.color }};white-space:nowrap;">{{ stop.meta.label }}</div>
              </div>
            </sc-for>
            <sc-if value="{{ historialEmpty }}" hint-placeholder-val="{{ false }}">
              <div style="text-align:center;color:#9AA19D;font-size:14px;padding:60px 20px;">Todavía no hay entregas resueltas.</div>
            </sc-if>
          </div>
        </sc-if>

        <!-- DETAIL -->
        <sc-if value="{{ isDetail }}" hint-placeholder-val="{{ false }}">
          <div style="padding:0 20px 20px;display:flex;flex-direction:column;gap:14px;">
            <div style="background:#fff;border-radius:16px;padding:16px;box-shadow:0 1px 3px rgba(20,32,29,0.05);">
              <div style="font-size:11px;font-weight:700;color:#8A928F;letter-spacing:0.3px;margin-bottom:6px;">DIRECCIÓN</div>
              <div style="font-size:15px;color:#14201D;font-weight:600;">{{ currentStop.address }}</div>
              <div style="font-size:13px;color:#7B837F;margin-top:2px;">{{ currentStop.window }}</div>
              <div style="height:1px;background:#EEF0EF;margin:14px 0;"></div>
              <div style="font-size:11px;font-weight:700;color:#8A928F;letter-spacing:0.3px;margin-bottom:6px;">VIAJE / TRANSPORTE</div>
              <div style="font-size:15px;color:#14201D;">{{ currentStop.tracking }} · {{ currentStop.transporte }}</div>
              <div style="height:1px;background:#EEF0EF;margin:14px 0;"></div>
              <div style="font-size:11px;font-weight:700;color:#8A928F;letter-spacing:0.3px;margin-bottom:6px;">UNIDAD</div>
              <div style="font-size:15px;color:#14201D;">{{ currentStop.phone }}</div>
              <div style="height:1px;background:#EEF0EF;margin:14px 0;"></div>
              <div style="font-size:11px;font-weight:700;color:#8A928F;letter-spacing:0.3px;margin-bottom:6px;">PEDIDO</div>
              <sc-for list="{{ currentStop.items }}" as="item" hint-placeholder-count="2">
                <div style="display:flex;justify-content:space-between;font-size:14px;color:#14201D;padding:4px 0;">
                  <span>{{ item.name }}</span>
                  <span style="color:#7B837F;">x{{ item.qty }}</span>
                </div>
              </sc-for>
            </div>

            <sc-if value="{{ currentStop.isResolved }}" hint-placeholder-val="{{ false }}">
              <div style="background:{{ currentStop.meta.bg }};border-radius:16px;padding:16px;">
                <div style="font-size:11px;font-weight:700;color:{{ currentStop.meta.color }};letter-spacing:0.3px;margin-bottom:6px;">{{ currentStop.meta.label }}</div>
                <div style="font-size:14px;color:#14201D;">{{ currentStop.resolvedSummary }}</div>
              </div>
            </sc-if>
          </div>
        </sc-if>

        <!-- CONFIRM FLOW -->
        <sc-if value="{{ isConfirm }}" hint-placeholder-val="{{ false }}">
          <div style="padding:0 20px 20px;display:flex;flex-direction:column;gap:16px;">
            <div style="font-size:13px;color:#7B837F;">{{ currentStop.customer }} · {{ currentStop.address }}</div>

            <div>
              <div style="font-size:13px;font-weight:700;color:#14201D;margin-bottom:8px;">Foto de entrega</div>
              <div onClick="{{ onPhotoClick }}" style="height:150px;border-radius:16px;border:1.5px dashed #C7CDCA;background:#fff;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;cursor:pointer;">
                <sc-if value="{{ noPhotoTaken }}" hint-placeholder-val="{{ true }}">
                  <svg width="30" height="26" viewBox="0 0 30 26" fill="none"><rect x="1" y="5" width="28" height="19" rx="3" stroke="#9AA19D" stroke-width="1.6"/><circle cx="15" cy="14.5" r="6" stroke="#9AA19D" stroke-width="1.6"/><rect x="10" y="1" width="8" height="4" rx="1.5" fill="#9AA19D"/></svg>
                  <div style="font-size:13px;color:#8A928F;">Tocar para tomar foto</div>
                </sc-if>
                <sc-if value="{{ photoTaken }}" hint-placeholder-val="{{ false }}">
                  <div style="width:36px;height:36px;border-radius:18px;background:{{ accentColor }};display:flex;align-items:center;justify-content:center;">
                    <svg width="16" height="12" viewBox="0 0 16 12" fill="none"><path d="M1 6l4.5 4.5L15 1" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  </div>
                  <div style="font-size:13px;color:#14201D;font-weight:600;">Foto capturada</div>
                  <div onClick="{{ onRetakePhoto }}" style="font-size:12px;color:{{ accentColor }};font-weight:700;">Volver a tomar</div>
                </sc-if>
              </div>
            </div>

            <div>
              <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;">
                <div style="font-size:13px;font-weight:700;color:#14201D;">Firma del cliente</div>
                <div onClick="{{ onClearSignature }}" style="font-size:12px;color:#8A928F;font-weight:600;cursor:pointer;">Borrar</div>
              </div>
              <div style="position:relative;height:170px;border-radius:16px;border:1.5px solid #E0E4E2;background:#fff;overflow:hidden;">
                <sc-if value="{{ noSignatureYet }}" hint-placeholder-val="{{ true }}">
                  <div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-size:13px;color:#C7CDCA;pointer-events:none;">Firmar aquí</div>
                </sc-if>
                <canvas ref="{{ sigCanvasRef }}" width="338" height="167" onPointerDown="{{ onPointerDown }}" onPointerMove="{{ onPointerMove }}" onPointerUp="{{ onPointerUp }}" onPointerLeave="{{ onPointerUp }}" style="touch-action:none;display:block;"></canvas>
              </div>
            </div>
          </div>
        </sc-if>

        <!-- FAIL FLOW -->
        <sc-if value="{{ isFail }}" hint-placeholder-val="{{ false }}">
          <div style="padding:0 20px 20px;display:flex;flex-direction:column;gap:16px;">
            <div style="font-size:13px;color:#7B837F;">{{ currentStop.customer }} · {{ currentStop.address }}</div>

            <div>
              <div style="font-size:13px;font-weight:700;color:#14201D;margin-bottom:8px;">Motivo</div>
              <div style="display:flex;flex-direction:column;gap:8px;">
                <sc-for list="{{ reasonsList }}" as="reason" hint-placeholder-count="4">
                  <div onClick="{{ reason.onSelect }}" style="display:flex;align-items:center;gap:10px;background:#fff;border-radius:12px;padding:12px 14px;border:1.5px solid {{ reason.borderColor }};cursor:pointer;">
                    <div style="width:18px;height:18px;border-radius:9px;border:1.5px solid {{ reason.dotBorder }};background:{{ reason.dotBg }};flex-shrink:0;"></div>
                    <div style="font-size:14px;color:#14201D;">{{ reason.label }}</div>
                  </div>
                </sc-for>
              </div>
            </div>

            <div>
              <div style="font-size:13px;font-weight:700;color:#14201D;margin-bottom:8px;">Nota (opcional)</div>
              <textarea value="{{ failNote }}" onChange="{{ onFailNoteChange }}" placeholder="Agregar un detalle..." style="width:100%;min-height:70px;border-radius:12px;border:1.5px solid #E0E4E2;padding:12px 14px;font-size:14px;font-family:inherit;color:#14201D;resize:none;"></textarea>
            </div>
          </div>
        </sc-if>
      </div>

      <!-- ============ FOOTERS ============ -->
      <sc-if value="{{ detailShowActions }}" hint-placeholder-val="{{ false }}">
        <div style="flex-shrink:0;padding:12px 20px 20px;display:flex;gap:10px;background:#F6F7F7;">
          <div onClick="{{ onStartFail }}" style="flex:1;text-align:center;padding:14px;border-radius:12px;border:1.5px solid #E0E4E2;font-weight:700;font-size:14px;color:#4C5652;cursor:pointer;">No se pudo entregar</div>
          <div onClick="{{ onStartConfirm }}" style="flex:1.3;text-align:center;padding:14px;border-radius:12px;background:{{ accentColor }};color:#fff;font-weight:700;font-size:14px;cursor:pointer;">Confirmar entrega</div>
        </div>
      </sc-if>

      <sc-if value="{{ isConfirm }}" hint-placeholder-val="{{ false }}">
        <div style="flex-shrink:0;padding:12px 20px 20px;background:#F6F7F7;">
          <div onClick="{{ onSubmitConfirm }}" style="text-align:center;padding:15px;border-radius:12px;background:{{ confirmButtonBg }};color:#fff;font-weight:700;font-size:15px;cursor:{{ confirmButtonCursor }};">Confirmar entrega</div>
        </div>
      </sc-if>

      <sc-if value="{{ isFail }}" hint-placeholder-val="{{ false }}">
        <div style="flex-shrink:0;padding:12px 20px 20px;background:#F6F7F7;">
          <div onClick="{{ onSubmitFail }}" style="text-align:center;padding:15px;border-radius:12px;background:{{ failButtonBg }};color:#fff;font-weight:700;font-size:15px;cursor:{{ failButtonCursor }};">Registrar no entrega</div>
        </div>
      </sc-if>

      <sc-if value="{{ showTabBar }}" hint-placeholder-val="{{ true }}">
        <div style="flex-shrink:0;display:flex;border-top:1px solid #E7EAE8;background:#fff;padding:8px 0 22px;">
          <div onClick="{{ onSetTabRuta }}" style="flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;cursor:pointer;">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M3 10l7-7 7 7M5 8.5V17h10V8.5" stroke="{{ tabRutaColor }}" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
            <div style="font-size:10.5px;font-weight:600;color:{{ tabRutaColor }};">Ruta</div>
          </div>
          <div onClick="{{ onSetTabMapa }}" style="flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;cursor:pointer;">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M10 18s6-5.5 6-10.5A6 6 0 004 7.5C4 12.5 10 18 10 18z" stroke="{{ tabMapaColor }}" stroke-width="1.8" stroke-linejoin="round"/><circle cx="10" cy="7.5" r="2.2" stroke="{{ tabMapaColor }}" stroke-width="1.6"/></svg>
            <div style="font-size:10.5px;font-weight:600;color:{{ tabMapaColor }};">Mapa</div>
          </div>
          <div onClick="{{ onSetTabHistorial }}" style="flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;cursor:pointer;">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="7.5" stroke="{{ tabHistorialColor }}" stroke-width="1.8"/><path d="M10 5.5V10l3 2" stroke="{{ tabHistorialColor }}" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
            <div style="font-size:10.5px;font-weight:600;color:{{ tabHistorialColor }};">Historial</div>
          </div>
        </div>
      </sc-if>

      <!-- TOAST -->
      <sc-if value="{{ showToast }}" hint-placeholder-val="{{ false }}">
        <div style="position:absolute;left:50%;bottom:100px;transform:translateX(-50%);background:#14201D;color:#fff;font-size:13px;font-weight:600;padding:11px 20px;border-radius:24px;box-shadow:0 6px 20px rgba(0,0,0,0.25);white-space:nowrap;">{{ toast }}</div>
      </sc-if>
      </sc-if><!-- /isLoggedIn -->
    </div>
  </x-import>
</div>

</x-dc>
<script type="text/x-dc" data-dc-script data-props="{&quot;$preview&quot;:{&quot;width&quot;:480,&quot;height&quot;:940},&quot;driverName&quot;:{&quot;editor&quot;:&quot;text&quot;,&quot;default&quot;:&quot;Martín Vega&quot;,&quot;tsType&quot;:&quot;string&quot;},&quot;accentColor&quot;:{&quot;editor&quot;:&quot;color&quot;,&quot;default&quot;:&quot;#0E7C6B&quot;,&quot;options&quot;:[&quot;#0E7C6B&quot;,&quot;#0F766E&quot;,&quot;#065F55&quot;,&quot;#1E7A5F&quot;],&quot;tsType&quot;:&quot;string&quot;},&quot;vehicleType&quot;:{&quot;editor&quot;:&quot;enum&quot;,&quot;default&quot;:&quot;Camioneta&quot;,&quot;options&quot;:[&quot;Moto&quot;,&quot;Camioneta&quot;],&quot;tsType&quot;:&quot;string&quot;}}">
const STATUS_META = {
  pendiente:    { label: 'Pendiente',     bg: '#F5EAD3', color: '#96650A' },
  entregado:    { label: 'Entregado',     bg: '#DCEFEA', color: '#0E7C6B' },
  no_entregado: { label: 'No entregado',  bg: '#F8DEDB', color: '#B23A2C' },
};

const FAIL_REASONS = ['Cliente ausente', 'Dirección incorrecta', 'Rechazado por el cliente', 'Otro motivo'];

const INITIAL_STOPS = [
  { id: 1, seq: 1, customer: 'Distribuidora Norte SRL', address: 'Av. Cabildo 2450, CABA', window: '09:00 – 10:00', phone: '(011) 4555-2301', status: 'entregado', resolvedAt: '09:15', tracking: 'VJ-DEMO1', transporte: 'Demo Transportes', items: [{ name: 'Cascos', qty: 12 }, { name: 'Guantes', qty: 20 }] },
  { id: 2, seq: 2, customer: 'Moto Repuestos Palermo', address: 'Av. Santa Fe 3800, CABA', window: '10:00 – 11:00', phone: '(011) 4777-1190', status: 'entregado', resolvedAt: '10:22', tracking: 'VJ-DEMO2', transporte: 'Demo Transportes', items: [{ name: 'Kit de frenos', qty: 8 }, { name: 'Filtros de aceite', qty: 15 }] },
  { id: 3, seq: 3, customer: 'Scooters del Sur', address: 'Av. Rivadavia 5200, CABA', window: '11:30 – 12:30', phone: '(011) 4901-6633', status: 'no_entregado', resolvedAt: '11:42', failReason: 'Cliente ausente', tracking: 'VJ-DEMO3', transporte: 'Demo Transportes', items: [{ name: 'Baterías de litio', qty: 6 }, { name: 'Cargadores', qty: 10 }] },
  { id: 4, seq: 4, customer: 'Bikes & Motos Belgrano', address: 'Av. Cabildo 1801, CABA', window: '12:30 – 13:30', phone: '(011) 4783-4420', status: 'pendiente', tracking: 'VJ-DEMO4', transporte: 'Demo Transportes', items: [{ name: 'Neumáticos', qty: 4 }, { name: 'Cámaras', qty: 6 }] },
  { id: 5, seq: 5, customer: 'Repuestos Flores', address: 'Av. Rivadavia 7100, CABA', window: '14:00 – 15:00', phone: '(011) 4611-8875', status: 'pendiente', tracking: 'VJ-DEMO5', transporte: 'Demo Transportes', items: [{ name: 'Kit de embrague', qty: 5 }, { name: 'Bujías', qty: 30 }] },
  { id: 6, seq: 6, customer: 'Taller San Telmo', address: 'Av. San Juan 950, CABA', window: '15:30 – 16:30', phone: '(011) 4300-9982', status: 'pendiente', tracking: 'VJ-DEMO6', transporte: 'Demo Transportes', items: [{ name: 'Aceite 4T', qty: 18 }, { name: 'Espejos', qty: 8 }] },
];

const PIN_POS = [
  { x: 16, y: 74 }, { x: 30, y: 46 }, { x: 48, y: 62 },
  { x: 62, y: 28 }, { x: 78, y: 44 }, { x: 88, y: 20 },
];

// Bridge to the La Emilia TMS (desktop) — reads the SAME cloud database it syncs to,
// so trips created on the desktop admin app show up here on any device (phone included).
// Falls back to its local-save localStorage key, then to demo data, if the cloud is unreachable.
const TMS_STORAGE_KEY = 'la_emilia_v10_local';
const TMS_FIREBASE_CONFIG = {
  apiKey: "AIzaSyCVpH9a8SZvxEKQjvaKCF88gOdaiU4CCJI",
  authDomain: "la-emilia-transporte.firebaseapp.com",
  databaseURL: "https://la-emilia-transporte-default-rtdb.firebaseio.com",
  projectId: "la-emilia-transporte",
  storageBucket: "la-emilia-transporte.firebasestorage.app",
  messagingSenderId: "242895943352",
  appId: "1:242895943352:web:7a09918810b76f96d2664d",
};

function hashPass(pass) {
  let h = 0;
  for (let i = 0; i < pass.length; i++) h = (Math.imul(31, h) + pass.charCodeAt(i)) | 0;
  return 'h' + Math.abs(h).toString(36);
}

class Component extends DCLogic {
  state = {
    authState: 'login',
    user: null,
    loginEmail: '',
    loginPass: '',
    loginError: null,
    loginBusy: false,
    tab: 'ruta',
    screen: null,
    selectedId: null,
    stops: INITIAL_STOPS,
    dataSource: 'demo',
    fbConnected: false,
    photoTaken: false,
    hasSignature: false,
    failReason: null,
    failNote: '',
    toast: null,
  };

  sigCanvasRef = React.createRef();
  drawing = false;

  componentDidMount() {
    this.refreshFromLocal();
    this._onStorage = (e) => { if (!e.key || e.key === TMS_STORAGE_KEY) this.refreshFromLocal(); };
    this._onVisible = () => { if (document.visibilityState === 'visible') this.refreshFromLocal(); };
    window.addEventListener('storage', this._onStorage);
    document.addEventListener('visibilitychange', this._onVisible);
    this.connectFirebase();
  }
  componentWillUnmount() {
    window.removeEventListener('storage', this._onStorage);
    document.removeEventListener('visibilitychange', this._onVisible);
    if (this._fbRef) { try { this._fbRef.off(); } catch (e) {} }
  }

  connectFirebase = (retries) => {
    retries = retries || 0;
    if (typeof firebase === 'undefined') {
      if (retries < 25) setTimeout(() => this.connectFirebase(retries + 1), 300);
      return;
    }
    try {
      const app = (firebase.apps && firebase.apps.length) ? firebase.apps[0] : firebase.initializeApp(TMS_FIREBASE_CONFIG);
      firebase.auth().signInAnonymously().catch(() => {});
      const ref = firebase.database().ref('app_state');
      this._fbRef = ref;
      this._driversRef = firebase.database().ref('drivers_db');
      ref.on('value', (snap) => {
        const data = snap.val();
        this.setState({ fbConnected: true });
        if (data) this.applyTMSData(data);
      }, (err) => { console.warn('La Emilia TMS: Firebase read error', err && err.message); });
    } catch (e) { console.warn('La Emilia TMS: Firebase connect error', e && e.message); }
  };

  onLoginEmailChange = (e) => this.setState({ loginEmail: e.target.value, loginError: null });
  onLoginPassChange = (e) => this.setState({ loginPass: e.target.value, loginError: null });
  onLoginKeyDown = (e) => { if (e.key === 'Enter') this.doLogin(); };
  onLoginSubmit = () => this.doLogin();

  doLogin = async () => {
    if (this.state.loginBusy) return;
    const email = (this.state.loginEmail || '').trim().toLowerCase();
    const pass = this.state.loginPass || '';
    if (!email || !pass) { this.setState({ loginError: 'Completá email y contraseña.' }); return; }
    if (!this._driversRef) { this.setState({ loginError: 'Conectando con el servidor, esperá un segundo e intentá de nuevo.' }); return; }
    this.setState({ loginBusy: true, loginError: null });
    try {
      const snap = await this._driversRef.once('value');
      const data = snap.val();
      const drivers = Array.isArray(data) ? data : (data && typeof data === 'object') ? Object.values(data).filter(Boolean) : [];
      const driver = drivers.find(u => u && (u.email || '').toLowerCase() === email);
      if (!driver) { this.setState({ loginBusy: false, loginError: 'Email no registrado como chofer.' }); return; }
      if (driver.status === 'inactivo') { this.setState({ loginBusy: false, loginError: 'Tu cuenta está inactiva.' }); return; }
      if (driver.passHash !== hashPass(pass)) { this.setState({ loginBusy: false, loginError: 'Contraseña incorrecta.' }); return; }
      this.setState({
        authState: 'in', loginBusy: false, loginPass: '',
        user: { id: driver.id, name: driver.name, email: driver.email },
      }, () => this.applyTMSData(this._lastTmsData));
    } catch (e) {
      this.setState({ loginBusy: false, loginError: 'Error de conexión: ' + (e && e.message ? e.message : 'intentá de nuevo.') });
    }
  };

  onLogout = () => this.setState({ authState: 'login', user: null, loginEmail: '', loginPass: '', tab: 'ruta', screen: null, selectedId: null });

  readTMSState() {
    try {
      const raw = localStorage.getItem(TMS_STORAGE_KEY);
      return raw ? JSON.parse(raw) : null;
    } catch (e) { return null; }
  }
  writeTMSState(data) {
    try { localStorage.setItem(TMS_STORAGE_KEY, JSON.stringify(data)); } catch (e) {}
  }
  norm(s) { return (s || '').toString().trim().toUpperCase(); }
  matchesDriver(trip, driverName) {
    const a = this.norm(trip.chofer), b = this.norm(driverName);
    if (!a || !b) return false;
    return a === b || a.includes(b) || b.includes(a);
  }

  buildStopsFromTMS(data, driverName) {
    if (!data) return null;
    const rawTrips = Array.isArray(data.trips) ? data.trips
      : (data.trips && typeof data.trips === 'object') ? Object.values(data.trips) : [];
    if (!rawTrips.length) return null;
    const trips = rawTrips.filter(t => t && t.estado !== 'cancelado' && this.matchesDriver(t, driverName));
    if (!trips.length) return null;
    trips.sort((a, b) => `${a.fecha || ''}${a.hora || ''}`.localeCompare(`${b.fecha || ''}${b.hora || ''}`));
    let seq = 0;
    const stops = [];
    trips.forEach(t => {
      const destList = (t.dests && t.dests.length) ? t.dests : [{ ciudad: t.localidad || 'Sin destino', pie: t.motos_pie || 0, cajon: t.motos_cajon || 0 }];
      destList.forEach((d, di) => {
        seq++;
        const items = [];
        if (d.pie) items.push({ name: 'Motos a pie', qty: d.pie });
        if (d.cajon) items.push({ name: 'Motos en cajón', qty: d.cajon });
        if (!items.length) items.push({ name: 'Carga general', qty: 1 });
        let status = 'pendiente';
        if (t.estado === 'entregado') status = 'entregado';
        else if (t.estado === 'no_entregado') status = 'no_entregado';
        stops.push({
          id: `${t.id}__${di}`,
          viajeId: t.id, destIdx: di,
          seq,
          customer: t.cliente || d.ciudad || 'Cliente sin nombre',
          address: d.ciudad ? `${d.ciudad}${d.desc ? ' · ' + d.desc : ''}` : (t.localidad || 'Sin dirección'),
          window: t.hora ? `${t.hora} hs` : 'Sin horario',
          phone: t.unidad ? `Unidad ${t.unidad}` : '—',
          status,
          resolvedAt: status !== 'pendiente' ? (t.hora || '') : undefined,
          failReason: t.estado === 'no_entregado' ? (t.obs || 'Sin especificar') : undefined,
          items,
          tracking: t.tracking,
          transporte: t.transporte || 'Sin transporte',
        });
      });
    });
    return stops;
  }

  // Local-save fallback path — only takes effect while Firebase hasn't connected yet (or is unreachable).
  refreshFromLocal = () => {
    if (this.state.fbConnected) return;
    this.applyTMSData(this.readTMSState());
  };

  applyTMSData = (data) => {
    this._lastTmsData = data;
    if (this.state.authState !== 'in' || !this.state.user) return;
    const driverName = this.state.user.name;
    const stops = this.buildStopsFromTMS(data, driverName);
    if (stops) {
      const prevIds = new Set((this.state.stops || []).map(s => s.id));
      const wasTms = this.state.dataSource === 'tms';
      const hasNew = stops.some(s => !prevIds.has(s.id));
      this.setState({ stops, dataSource: 'tms' });
      if (wasTms && hasNew) this.pushToast('Nuevo viaje asignado');
    } else if (this.state.dataSource !== 'demo') {
      this.setState({ stops: INITIAL_STOPS, dataSource: 'demo' });
    }
  };

  syncBackToTMS(viajeId) {
    const related = this.state.stops.filter(s => s.viajeId === viajeId);
    const allResolved = related.length > 0 && related.every(s => s.status !== 'pendiente');
    if (!allResolved) return;
    const anyFailed = related.some(s => s.status === 'no_entregado');
    const newEstado = anyFailed ? 'no_entregado' : 'entregado';
    const evt = anyFailed ? 'Entrega fallida registrada desde app móvil' : 'Entrega confirmada desde app móvil';

    if (this.state.fbConnected && this._fbRef) {
      this._fbRef.child('trips').once('value').then(snap => {
        const val = snap.val();
        const trips = Array.isArray(val) ? val : (val && typeof val === 'object') ? Object.values(val) : [];
        const idx = trips.findIndex(t => t && t.id === viajeId);
        if (idx < 0) return;
        trips[idx] = { ...trips[idx], estado: newEstado, timeline: [...(trips[idx].timeline || []), { evt, ts: new Date().toISOString() }] };
        this._fbRef.child('trips').set(trips);
        this._fbRef.child('_ts').set(Date.now());
      }).catch(e => console.warn('La Emilia TMS: sync-back error', e && e.message));
      return;
    }

    const data = this.readTMSState();
    if (!data || !Array.isArray(data.trips)) return;
    const trip = data.trips.find(t => t.id === viajeId);
    if (!trip) return;
    trip.estado = newEstado;
    trip.timeline = trip.timeline || [];
    trip.timeline.push({ evt, ts: new Date().toISOString() });
    this.writeTMSState(data);
  }

  hexToRgba(hex, alpha) {
    const h = hex.replace('#', '');
    const r = parseInt(h.substring(0, 2), 16), g = parseInt(h.substring(2, 4), 16), b = parseInt(h.substring(4, 6),16);
    return `rgba(${r},${g},${b},${alpha})`;
  }

  setTab = (tab) => this.setState({ tab, screen: null, selectedId: null });
  openDetail = (id) => this.setState({ screen: 'detail', selectedId: id });
  closeDetail = () => this.setState({ screen: null, selectedId: null });
  startConfirm = () => this.setState({ screen: 'confirm', photoTaken: false, hasSignature: false });
  startFail = () => this.setState({ screen: 'fail', failReason: null, failNote: '', photoTaken: false });
  backToDetail = () => this.setState({ screen: 'detail' });
  selectReason = (reason) => this.setState({ failReason: reason });
  onFailNoteChange = (e) => this.setState({ failNote: e.target.value });
  onNavigate = () => this.pushToast('Abriendo navegación GPS…');

  onPhotoClick = () => this.setState({ photoTaken: true });
  onRetakePhoto = (e) => { e.stopPropagation(); this.setState({ photoTaken: false }); };

  onPointerDown = (e) => {
    const canvas = this.sigCanvasRef.current;
    if (!canvas) return;
    const rect = canvas.getBoundingClientRect();
    const ctx = canvas.getContext('2d');
    ctx.strokeStyle = '#14201D';
    ctx.lineWidth = 2.5;
    ctx.lineCap = 'round';
    ctx.lineJoin = 'round';
    ctx.beginPath();
    ctx.moveTo(e.clientX - rect.left, e.clientY - rect.top);
    this.drawing = true;
  };
  onPointerMove = (e) => {
    if (!this.drawing) return;
    const canvas = this.sigCanvasRef.current;
    if (!canvas) return;
    const rect = canvas.getBoundingClientRect();
    const ctx = canvas.getContext('2d');
    ctx.lineTo(e.clientX - rect.left, e.clientY - rect.top);
    ctx.stroke();
    if (!this.state.hasSignature) this.setState({ hasSignature: true });
  };
  onPointerUp = () => { this.drawing = false; };
  onClearSignature = () => {
    const canvas = this.sigCanvasRef.current;
    if (canvas) canvas.getContext('2d').clearRect(0, 0, canvas.width, canvas.height);
    this.setState({ hasSignature: false });
  };

  pushToast(msg) {
    clearTimeout(this._toastTimer);
    this.setState({ toast: msg });
    this._toastTimer = setTimeout(() => this.setState({ toast: null }), 2200);
  }

  submitConfirm = () => {
    if (!(this.state.photoTaken && this.state.hasSignature)) return;
    const id = this.state.selectedId;
    const stop = this.state.stops.find(s => s.id === id);
    const time = new Date().toLocaleTimeString('es-AR', { hour: '2-digit', minute: '2-digit' });
    const dataSource = this.state.dataSource;
    this.setState(s => ({
      stops: s.stops.map(st => st.id === id ? { ...st, status: 'entregado', resolvedAt: time } : st),
      screen: null, selectedId: null,
    }), () => { if (stop && dataSource === 'tms') this.syncBackToTMS(stop.viajeId); });
    this.pushToast('Entrega confirmada');
  };

  submitFail = () => {
    if (!this.state.failReason) return;
    const id = this.state.selectedId;
    const stop = this.state.stops.find(s => s.id === id);
    const time = new Date().toLocaleTimeString('es-AR', { hour: '2-digit', minute: '2-digit' });
    const reason = this.state.failReason;
    const dataSource = this.state.dataSource;
    this.setState(s => ({
      stops: s.stops.map(st => st.id === id ? { ...st, status: 'no_entregado', resolvedAt: time, failReason: reason } : st),
      screen: null, selectedId: null,
    }), () => { if (stop && dataSource === 'tms') this.syncBackToTMS(stop.viajeId); });
    this.pushToast('Registrado como no entregado');
  };

  renderVals() {
    const accentColor = this.props.accentColor ?? '#0E7C6B';
    const vehicleType = this.props.vehicleType ?? 'Moto';
    const routeDate = 'Hoy, 9 de julio';
    const { tab, screen, stops, selectedId, photoTaken, hasSignature, failReason, failNote, toast, dataSource, fbConnected, user } = this.state;
    const driverName = user ? user.name : (this.props.driverName ?? 'Martín Vega');
    const isLogin = this.state.authState === 'login';
    const isLoggedIn = this.state.authState === 'in';
    const fbStatusColor = fbConnected ? accentColor : '#C7CDCA';
    const fbStatusLabel = fbConnected ? 'Conectado al TMS' : 'Conectando…';
    const loginBusy = this.state.loginBusy;
    const loginButtonLabel = loginBusy ? 'VERIFICANDO…' : 'INGRESAR';
    const loginButtonBg = loginBusy ? '#C7CDCA' : accentColor;
    const loginButtonCursor = loginBusy ? 'default' : 'pointer';

    const isRuta = tab === 'ruta' && !screen;
    const isMapa = tab === 'mapa' && !screen;
    const isHistorial = tab === 'historial' && !screen;
    const isDetail = screen === 'detail';
    const isConfirm = screen === 'confirm';
    const isFail = screen === 'fail';
    const showTabBar = !screen;

    const withMeta = (s) => ({ ...s, meta: STATUS_META[s.status] });

    const nextPendingId = (stops.find(s => s.status === 'pendiente') || {}).id;

    const routeList = stops.map(s => {
      const m = withMeta(s);
      const isNext = s.id === nextPendingId;
      return {
        ...m,
        isNext,
        borderColor: isNext ? accentColor : '#EEF0EF',
        numBg: this.hexToRgba(m.meta.color, 0.14),
        numColor: m.meta.color,
        onOpen: () => this.openDetail(s.id),
      };
    });

    const resolvedStops = stops.filter(s => s.status !== 'pendiente');
    const historialList = resolvedStops.map(s => ({ ...withMeta(s), onOpen: () => this.openDetail(s.id) }));
    const historialEmpty = historialList.length === 0;

    const deliveredCount = stops.filter(s => s.status === 'entregado').length;
    const resolvedCount = stops.filter(s => s.status !== 'pendiente').length;
    const progressLabel = `${resolvedCount} de ${stops.length} resueltas`;
    const progressPct = Math.round((resolvedCount / stops.length) * 100);
    const progressPctLabel = `${progressPct}%`;
    const progressBarWidth = `${progressPct}%`;

    const nextStopRaw = stops.find(s => s.id === nextPendingId);
    const hasNextStop = !!nextStopRaw;
    const nextStop = nextStopRaw || {};

    const pinColor = (s) => s.status === 'entregado' ? accentColor : s.status === 'no_entregado' ? '#C0392B' : '#C98A1A';
    const mapPins = stops.map((s, i) => {
      const pos = PIN_POS[i % PIN_POS.length];
      const isCurrent = s.id === nextPendingId;
      const size = isCurrent ? 30 : 22;
      return {
        seq: s.seq,
        style: {
          position: 'absolute', left: `${pos.x}%`, top: `${pos.y}%`, transform: 'translate(-50%,-50%)',
          width: size, height: size, borderRadius: size / 2, background: pinColor(s),
          color: '#fff', fontSize: isCurrent ? 13 : 11, fontWeight: 700,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          boxShadow: isCurrent ? `0 0 0 5px ${this.hexToRgba(pinColor(s), 0.25)}` : '0 1px 4px rgba(0,0,0,0.15)',
          border: '2px solid #fff',
        },
      };
    });
    const mapPolylinePoints = PIN_POS.map(p => `${p.x},${p.y}`).join(' ');

    let currentStop = null;
    if (selectedId != null) {
      const raw = stops.find(s => s.id === selectedId);
      if (raw) {
        const m = withMeta(raw);
        const isResolved = raw.status !== 'pendiente';
        let resolvedSummary = '';
        if (raw.status === 'entregado') resolvedSummary = `Entregado a las ${raw.resolvedAt} · Firma registrada`;
        if (raw.status === 'no_entregado') resolvedSummary = `Intentado a las ${raw.resolvedAt} · Motivo: ${raw.failReason}`;
        currentStop = { ...m, isResolved, resolvedSummary };
      }
    }
    const detailShowActions = isDetail && currentStop && !currentStop.isResolved;

    const reasonsList = FAIL_REASONS.map(r => ({
      key: r, label: r,
      selected: r === failReason,
      borderColor: r === failReason ? accentColor : '#EEF0EF',
      dotBg: r === failReason ? accentColor : '#fff',
      dotBorder: r === failReason ? accentColor : '#C7CDCA',
      onSelect: () => this.selectReason(r),
    }));

    const isTms = dataSource === 'tms';
    let syncLabel;
    if (isTms) syncLabel = `Sincronizado con el TMS (nube) · ${stops[0] && stops[0].transporte || ''}`;
    else if (fbConnected) syncLabel = `Conectado a la nube — sin viajes asignados a "${driverName}"`;
    else syncLabel = `Modo demostración — sin conexión al TMS`;
    const syncDotColor = isTms ? accentColor : (fbConnected ? '#5AADFF' : '#C7CDCA');

    const sigCanvasRef = this.sigCanvasRef;
    const canSubmitConfirm = photoTaken && hasSignature;
    const canSubmitFail = !!failReason;
    const noPhotoTaken = !photoTaken;
    const noSignatureYet = !hasSignature;

    return {
      accentColor, driverName, vehicleType, routeDate,
      isLogin, isLoggedIn, fbStatusColor, fbStatusLabel,
      loginEmail: this.state.loginEmail, loginPass: this.state.loginPass,
      onLoginEmailChange: this.onLoginEmailChange, onLoginPassChange: this.onLoginPassChange,
      onLoginKeyDown: this.onLoginKeyDown, onLoginSubmit: this.onLoginSubmit,
      loginError: this.state.loginError, loginButtonLabel, loginButtonBg, loginButtonCursor,
      onLogout: this.onLogout,
      syncLabel, syncDotColor,
      isRuta, isMapa, isHistorial, isDetail, isConfirm, isFail, showTabBar,
      routeList, historialList, historialEmpty,
      progressLabel, progressPctLabel, progressBarWidth,
      hasNextStop, nextStop, mapPins, mapPolylinePoints,
      currentStop: currentStop || { items: [] },
      detailShowActions,
      photoTaken, hasSignature, noPhotoTaken, noSignatureYet, sigCanvasRef,
      onPhotoClick: this.onPhotoClick, onRetakePhoto: this.onRetakePhoto,
      onPointerDown: this.onPointerDown, onPointerMove: this.onPointerMove, onPointerUp: this.onPointerUp,
      onClearSignature: this.onClearSignature,
      reasonsList, failNote,
      onFailNoteChange: this.onFailNoteChange,
      confirmButtonBg: canSubmitConfirm ? accentColor : '#C7CDCA',
      confirmButtonCursor: canSubmitConfirm ? 'pointer' : 'default',
      failButtonBg: canSubmitFail ? '#B23A2C' : '#C7CDCA',
      failButtonCursor: canSubmitFail ? 'pointer' : 'default',
      onStartConfirm: this.startConfirm, onStartFail: this.startFail,
      onCloseDetail: this.closeDetail, onBackToDetail: this.backToDetail,
      onSubmitConfirm: this.submitConfirm, onSubmitFail: this.submitFail,
      onSetTabRuta: () => this.setTab('ruta'),
      onSetTabMapa: () => this.setTab('mapa'),
      onSetTabHistorial: () => this.setTab('historial'),
      tabRutaColor: isRuta ? accentColor : '#9AA19D',
      tabMapaColor: isMapa ? accentColor : '#9AA19D',
      tabHistorialColor: isHistorial ? accentColor : '#9AA19D',
      toast, showToast: !!toast,
    };
  }
}
</script>
</body>
</html>
